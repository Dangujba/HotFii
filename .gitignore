*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Private planning transcript, 850 KB. Not part of the product.
/conversation.md

# Empty leftover from the initial scaffold.
/hotfii-scaffold

# Database dumps produced by the backup commands in
# docs/DOCKER_VPS_DEPLOYMENT.md.
*.dump
/hotfii-uploads.tar.gz

# Throwaway VPS helper scripts. They hold host credentials, so they must
# never reach the repository.
/.vps_*.py

# Manual pre-edit backup copies made while patching live on the VPS
# (PortalController.php.before-omada-browserauth and friends), plus the
# archives used to move them off the server. Useful to read, never to commit.
*.before-*
/hotfii-sidecars.tgz
/hotfii-full.tgz
/*.patch
