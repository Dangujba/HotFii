@extends('layouts.auth') @section('title','Sign in') @section('content')<h1 class="h3 fw-bold">Welcome back</h1><p class="text-secondary mb-4">Sign in to manage your network.</p><form method="POST" action="{{ route('login') }}">@csrf<div class="mb-3"><label class="form-label">Email address</label><input type="email" name="email" value="{{ old('email') }}" class="form-control form-control-lg" required autofocus></div><div class="mb-3"><label class="form-label">Password</label><input type="password" name="password" class="form-control form-control-lg" required></div><div class="form-check mb-4"><input type="checkbox" name="remember" class="form-check-input" id="remember"><label for="remember" class="form-check-label">Keep me signed in</label></div><button class="btn btn-hotfii btn-lg w-100">Sign in</button></form><p class="text-center mt-4 mb-0">New to HotFii? <a href="{{ route('register') }}">Create an organization</a></p>@endsection